// Compiled in every tier archive to validate the stable C ABI before the corresponding kernel is
// introduced. The declarations themselves intentionally have no definitions here: subsequent
// kernel tasks provide one strong definition per declared entry point.

#include "eidos_arm.h"
