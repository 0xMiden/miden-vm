
## miden::core::sys::pvm::aux_trace
| Procedure | Description |
| ----------- | ------------- |
| scatter_aux_bus_boundary | Moves each normalized boundary sum from its proof-order slot to its canonical instance-order<br />slot.<br /><br />The proof submits one sum per chiplet ordered by trace height, so the absorb above stores them<br />in that order. The constraint circuit instead reads chiplet `k`'s sum at<br />`AUX_BUS_BOUNDARY_PTR + 2k`, the address `InputKey::AuxBusBoundary(k)` names, with `k` the<br />canonical instance index. Positions come from the table `pvm::stage_proof_order_positions`<br />stages once per proof, not from a per-call ranking of the heights.<br /><br />Every sum is read onto the operand stack before any is written back, so the region is permuted<br />in place without scratch memory. The AIR heights and their proof-order positions must already<br />be staged; the sums must already be absorbed.<br /><br />Input:  [...]<br />Output: [...]<br /> |
| observe_aux_trace | Observes the PVM auxiliary trace commitment and its ten normalized LogUp values.<br /><br />The advice provider supplies the commitment followed by each chiplet's exposed value in proof<br />order. Every chiplet exposes one `sigma_prime = sigma / n`, which is weighted by that chiplet's<br />trace length before closing the external LogUp identity.<br /><br />The stream is 24 base felts: the four-felt commitment followed by twenty LogUp-value<br />coordinates. It fills three Eidos rate blocks exactly, matching the lifted-STARK transcript<br />before it samples composition challenges.<br /><br />Once absorbed, the values are moved to the canonical addresses the constraint circuit reads<br />them from.<br /><br />The hook also adds the verifier-side consumes for the fixed uint/curve environment and asserts<br />`fixed_boundary_correction + sum_i(n_i * sigma_prime_i) == 0` coordinate-wise.<br /><br />Precondition:  input_len=0 (the main commitment was flushed before this hook).<br />Postcondition: input_len=4, output_len=0.<br /><br />Input:  [...]<br />Output: [...]<br /> |
