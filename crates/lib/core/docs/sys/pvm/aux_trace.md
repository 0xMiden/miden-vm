
## miden::core::sys::pvm::aux_trace
| Procedure | Description |
| ----------- | ------------- |
| observe_aux_trace | Observes the PVM auxiliary trace commitment and its twelve normalized LogUp values.<br /><br />The advice provider supplies the commitment followed by each AIR's exposed values in proof<br />order. Eight AIRs expose one `sigma_prime = sigma / n`; EidosCompression and BytePairAnd8<br />expose two apiece. Every value is weighted by its parent AIR's trace length before closing the<br />external LogUp identity.<br /><br />The stream is 28 base felts: the four-felt commitment followed by twenty-four LogUp-value<br />coordinates. Three Eidos blocks are compressed and the final four felts remain buffered,<br />matching the lifted-STARK transcript before it samples composition challenges.<br /><br />The hook also adds the verifier-side consumes for the fixed uint/curve environment and asserts<br />`fixed_boundary_correction + sum_i(n_i * sum_j(sigma_prime_ij)) == 0` coordinate-wise.<br /><br />Preconditions:<br />- `pvm::load_air_context` populated the per-AIR heights and auxiliary-value pointers;<br />- input_len=0 (the main commitment was flushed before this hook).<br />Postcondition: input_len=4, output_len=0.<br /><br />Input:  [...]<br />Output: [...]<br /> |
