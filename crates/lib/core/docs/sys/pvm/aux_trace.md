
## miden::core::sys::pvm::aux_trace
| Procedure | Description |
| ----------- | ------------- |
| observe_aux_trace | Observes the PVM auxiliary trace commitment and its ten normalized LogUp values.<br /><br />The advice provider supplies the commitment followed by each AIR's exposed values in proof<br />order. Every AIR exposes one `sigma_prime = sigma / n`, which is weighted by that AIR's trace<br />length before closing the external LogUp identity.<br /><br />The stream is 24 base felts: the four-felt commitment followed by twenty LogUp-value<br />coordinates. It fills three Eidos rate blocks exactly, matching the lifted-STARK transcript<br />before it samples composition challenges.<br /><br />The hook also adds the verifier-side consumes for the fixed uint/curve environment and asserts<br />`fixed_boundary_correction + sum_i(n_i * sigma_prime_i) == 0` coordinate-wise.<br /><br />Preconditions:<br />- `pvm::load_air_context` populated the per-AIR heights and auxiliary-value pointers;<br />- input_len=0 (the main commitment was flushed before this hook).<br />Postcondition: input_len=0, output_len=0.<br /><br />Input:  [...]<br />Output: [...]<br /> |
