
## miden::core::sys::vm::aux_trace
| Procedure | Description |
| ----------- | ------------- |
| push_canonical_log_heights | Push the four AIR log heights in canonical instance order: Core, Chiplets,<br />EidosCompression, And8Lookup.<br /><br />The order the values these heights weight arrived in does not appear here: the boundary sums<br />are moved to their canonical addresses before the weighted sum reads them, so pairing is by<br />canonical instance index on both sides.<br /><br />Input:  [...]<br />Output: [log_0, log_1, log_2, log_3, ...]<br /> |
| scatter_aux_bus_boundary | Moves each normalized boundary sum from its proof-order slot to its canonical instance-order<br />slot.<br /><br />The proof submits one sum per AIR ordered by trace height, so the absorb above stores them in<br />that order. The constraint circuit instead reads AIR `k`'s sum at `AUX_BUS_BOUNDARY_PTR + 2k`,<br />the address `InputKey::AuxBusBoundary(k)` names, with `k` the canonical instance index.<br /><br />Every sum is read onto the operand stack before any is written back, so the region is permuted<br />in place without scratch memory. The AIR heights must already be stored; the sums must already<br />be absorbed.<br /><br />Input:  [...]<br />Output: [...]<br /> |
| observe_aux_trace | Observes the auxiliary trace for the Miden VM AIR.<br /><br />Draws auxiliary randomness, absorbs the auxiliary trace commitment and four normalized<br />boundary sums, moves the sums to the canonical addresses the constraint circuit reads them<br />from, and checks the modeless outer-LogUp boundary identity there.<br /><br />The advice provider supplies:<br />[commitment, sigma_prime_0, sigma_prime_1, sigma_prime_2, sigma_prime_3]<br /><br />Input:  [...]<br />Output: [...]<br /> |
