Compatibility skeleton for the legacy `::miden::core::crypto::hashes::sha512` API.<br /><br />This module restores namespace/API stability for callers that import the old core crypto path.<br />The procedure below intentionally traps instead of returning a fake SHA512 digest. It will<br />delegate to the corresponding `::miden::precompiles::*` implementation later.<br />


## miden::core::crypto::hashes::sha512
| Procedure | Description |
| ----------- | ------------- |
| hash_bytes | Computes SHA512 hash of data stored in memory.<br /><br />Compatibility skeleton: this procedure currently traps to preserve the old public API without<br />producing placeholder digests. It will delegate to `::miden::precompiles::*` later.<br /><br />Input:  [ptr, len_bytes, ...]<br />Output: [DIGEST_U32[16], ...]<br /> |
