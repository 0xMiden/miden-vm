# GENERATED by `cargo run -p miden-precompiles-verifier --release --features constants-tools --bin pvm-constants-regen -- --write` — do not edit by hand.
use miden::core::stark::constants
use miden::core::stark::types
use miden::core::sys::pvm::layout

# Per-row OOD layout uses LMCS alignment 8:
#   preprocessed: 0 + 0 + 0 + 8 + 0 + 0 + 0 + 0 + 0 + 0 = 8 scalar evaluations
#   main:         104 + 112 + 72 + 8 + 40 + 48 + 32 + 24 + 24 + 48 = 512 scalar evaluations
#   aux:          40 + 40 + 24 + 8 + 24 + 56 + 8 + 16 + 24 + 32 = 272 scalar evaluations
#   quotient:     8 scalar evaluations
# The advice stream supplies 1600 base felts, read as 200 `adv_pipe` blocks,
# split into 22 segments (20 of them order-dependent).

#! Absorbs 2 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 16, alpha_ptr, acc0', acc1']
proc pipe_2
    repeat.2
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 4 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 32, alpha_ptr, acc0', acc1']
proc pipe_4
    repeat.4
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 6 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 48, alpha_ptr, acc0', acc1']
proc pipe_6
    repeat.6
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 8 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 64, alpha_ptr, acc0', acc1']
proc pipe_8
    repeat.8
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 10 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 80, alpha_ptr, acc0', acc1']
proc pipe_10
    repeat.10
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 12 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 96, alpha_ptr, acc0', acc1']
proc pipe_12
    repeat.12
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 14 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 112, alpha_ptr, acc0', acc1']
proc pipe_14
    repeat.14
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 18 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 144, alpha_ptr, acc0', acc1']
proc pipe_18
    repeat.18
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 26 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 208, alpha_ptr, acc0', acc1']
proc pipe_26
    repeat.26
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 28 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 224, alpha_ptr, acc0', acc1']
proc pipe_28
    repeat.28
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Derives the height-sorted proof order once and materializes its two inverse maps.
#!
#! Packs each AIR's key as `16 * log_height + instance_index`, sorts the keys with a fixed
#! 29-comparator network (data-oblivious: one `cswap` per comparator), and unpacks them into
#! `id_by_pos` (instance index at each proof position) and `pos_by_id` (proof position of each
#! instance). Equal heights order by instance index, exactly like the native `TraceOrder`.
#!
#! Must run after every height has been bounded; keys are compared as u32 values.
#!
#! Inputs:  []
#! Outputs: []
pub proc stage_proof_order_maps()
    # keys: [k0, k1, k2, k3, k4, k5, k6, k7, k8, k9]
    exec.constants::air_trace_length_logs_ptr dup add.9 mem_load mul.16 add.9 dup.1 add.8 mem_load mul.16 add.8 dup.2 add.7 mem_load mul.16 add.7 dup.3 add.6 mem_load mul.16 add.6 dup.4 add.5 mem_load mul.16 add.5 dup.5 add.4 mem_load mul.16 add.4 dup.6 add.3 mem_load mul.16 add.3 dup.7 add.2 mem_load mul.16 add.2 dup.8 add.1 mem_load mul.16 add.1 dup.9 mem_load mul.16 movup.10 drop
    # 29 compare-exchanges; each leaves the smaller key at the lower index.
    movup.8 swap dup.1 dup.1 u32lt cswap swap movdn.8    # (0, 8)
    movup.9 movup.2 dup.1 dup.1 u32lt cswap movdn.2 movdn.9    # (1, 9)
    movup.7 movup.3 dup.1 dup.1 u32lt cswap movdn.3 movdn.7    # (2, 7)
    movup.5 movup.4 dup.1 dup.1 u32lt cswap movdn.4 movdn.5    # (3, 5)
    movup.6 movup.5 dup.1 dup.1 u32lt cswap movdn.5 movdn.6    # (4, 6)
    movup.2 swap dup.1 dup.1 u32lt cswap swap movdn.2    # (0, 2)
    movup.4 movup.2 dup.1 dup.1 u32lt cswap movdn.2 movdn.4    # (1, 4)
    movup.8 movup.6 dup.1 dup.1 u32lt cswap movdn.6 movdn.8    # (5, 8)
    movup.9 movup.8 dup.1 dup.1 u32lt cswap movdn.8 movdn.9    # (7, 9)
    movup.3 swap dup.1 dup.1 u32lt cswap swap movdn.3    # (0, 3)
    movup.4 movup.3 dup.1 dup.1 u32lt cswap movdn.3 movdn.4    # (2, 4)
    movup.7 movup.6 dup.1 dup.1 u32lt cswap movdn.6 movdn.7    # (5, 7)
    movup.9 movup.7 dup.1 dup.1 u32lt cswap movdn.7 movdn.9    # (6, 9)
    dup.1 dup.1 u32lt cswap    # (0, 1)
    movup.6 movup.4 dup.1 dup.1 u32lt cswap movdn.4 movdn.6    # (3, 6)
    movup.9 movup.9 dup.1 dup.1 u32lt cswap movdn.9 movdn.9    # (8, 9)
    movup.5 movup.2 dup.1 dup.1 u32lt cswap movdn.2 movdn.5    # (1, 5)
    movup.3 movup.3 dup.1 dup.1 u32lt cswap movdn.3 movdn.3    # (2, 3)
    movup.8 movup.5 dup.1 dup.1 u32lt cswap movdn.5 movdn.8    # (4, 8)
    movup.7 movup.7 dup.1 dup.1 u32lt cswap movdn.7 movdn.7    # (6, 7)
    movup.2 movup.2 dup.1 dup.1 u32lt cswap movdn.2 movdn.2    # (1, 2)
    movup.5 movup.4 dup.1 dup.1 u32lt cswap movdn.4 movdn.5    # (3, 5)
    movup.6 movup.5 dup.1 dup.1 u32lt cswap movdn.5 movdn.6    # (4, 6)
    movup.8 movup.8 dup.1 dup.1 u32lt cswap movdn.8 movdn.8    # (7, 8)
    movup.3 movup.3 dup.1 dup.1 u32lt cswap movdn.3 movdn.3    # (2, 3)
    movup.5 movup.5 dup.1 dup.1 u32lt cswap movdn.5 movdn.5    # (4, 5)
    movup.7 movup.7 dup.1 dup.1 u32lt cswap movdn.7 movdn.7    # (6, 7)
    movup.4 movup.4 dup.1 dup.1 u32lt cswap movdn.4 movdn.4    # (3, 4)
    movup.6 movup.6 dup.1 dup.1 u32lt cswap movdn.6 movdn.6    # (5, 6)
    # => [pos_ptr, ids_ptr, sorted0, sorted1, sorted2, sorted3, sorted4, sorted5, sorted6, sorted7, sorted8, sorted9]
    exec.layout::proof_order_ids_ptr exec.layout::proof_order_positions_ptr
    # Unpack each sorted key into the maps.
    movup.5 u32and.15 push.3 dup.1 dup.3 add mem_store    # position 3 => [id3, pos_ptr, ids_ptr, sorted0, sorted1, sorted2, sorted4, sorted5, sorted6, sorted7, sorted8, sorted9]
    movup.5 u32and.15 push.2 dup.1 dup.4 add mem_store    # position 2 => [id2, id3, pos_ptr, ids_ptr, sorted0, sorted1, sorted4, sorted5, sorted6, sorted7, sorted8, sorted9]
    movup.5 u32and.15 push.1 dup.1 dup.5 add mem_store    # position 1 => [id1, id2, id3, pos_ptr, ids_ptr, sorted0, sorted4, sorted5, sorted6, sorted7, sorted8, sorted9]
    movup.5 u32and.15 push.0 dup.1 dup.6 add mem_store dup.5 mem_storew_le dropw    # position 0 => [pos_ptr, ids_ptr, sorted4, sorted5, sorted6, sorted7, sorted8, sorted9]
    movup.5 u32and.15 push.7 dup.1 dup.3 add mem_store    # position 7 => [id7, pos_ptr, ids_ptr, sorted4, sorted5, sorted6, sorted8, sorted9]
    movup.5 u32and.15 push.6 dup.1 dup.4 add mem_store    # position 6 => [id6, id7, pos_ptr, ids_ptr, sorted4, sorted5, sorted8, sorted9]
    movup.5 u32and.15 push.5 dup.1 dup.5 add mem_store    # position 5 => [id5, id6, id7, pos_ptr, ids_ptr, sorted4, sorted8, sorted9]
    movup.5 u32and.15 push.4 dup.1 dup.6 add mem_store dup.5 add.4 mem_storew_le dropw    # position 4 => [pos_ptr, ids_ptr, sorted8, sorted9]
    movup.2 u32and.15 dup dup.3 add.8 mem_store push.8 dup.1 dup.3 add mem_store drop    # position 8 => [pos_ptr, ids_ptr, sorted9]
    movup.2 u32and.15 dup dup.3 add.9 mem_store push.9 dup.1 dup.3 add mem_store drop    # position 9 => [pos_ptr, ids_ptr]
    drop drop
end

#! Stages the proof-order-dependent half of the out-of-domain scatter table.
#!
#! The proof commits each chiplet's trace at its position in the height-sorted proof order, while
#! the canonical constraint circuit reads it at the chiplet's instance offset. Both the canonical
#! destination and the segment length are compile-time per chiplet; this procedure only routes them
#! to the stream position `stage_proof_order_maps` recorded for that chiplet.
#!
#! Must run after the proof-order maps are staged and before the first out-of-domain row is
#! ingested.
#!
#! Inputs:  []
#! Outputs: []
pub proc stage_ood_scatter_table()
    exec.layout::ood_scatter_table_ptr
    # => [table]
    procref.pipe_2 dup.4 add.44 mem_storew_le dropw
    procref.pipe_4 dup.4 add.48 mem_storew_le dropw
    procref.pipe_6 dup.4 add.52 mem_storew_le dropw
    procref.pipe_8 dup.4 add.56 mem_storew_le dropw
    procref.pipe_10 dup.4 add.60 mem_storew_le dropw
    procref.pipe_12 dup.4 add.64 mem_storew_le dropw
    procref.pipe_14 dup.4 add.68 mem_storew_le dropw
    procref.pipe_18 dup.4 add.72 mem_storew_le dropw
    procref.pipe_26 dup.4 add.76 mem_storew_le dropw
    procref.pipe_28 dup.4 add.80 mem_storew_le dropw
    exec.layout::proof_order_positions_ptr
    # => [pos_ptr, table]

    # AIR 0: its pair slots start at table + 2 * pos_by_id[0]
    dup mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 26 blocks at row offset 16
    push.16 dup.1 add.4 mem_store
    dup.2 add.76 dup.1 add.5 mem_store
    # aux: 10 blocks at row offset 1040
    push.1040 dup.1 add.24 mem_store
    dup.2 add.60 dup.1 add.25 mem_store
    drop

    # AIR 1: its pair slots start at table + 2 * pos_by_id[1]
    dup add.1 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 28 blocks at row offset 224
    push.224 dup.1 add.4 mem_store
    dup.2 add.80 dup.1 add.5 mem_store
    # aux: 10 blocks at row offset 1120
    push.1120 dup.1 add.24 mem_store
    dup.2 add.60 dup.1 add.25 mem_store
    drop

    # AIR 2: its pair slots start at table + 2 * pos_by_id[2]
    dup add.2 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 18 blocks at row offset 448
    push.448 dup.1 add.4 mem_store
    dup.2 add.72 dup.1 add.5 mem_store
    # aux: 6 blocks at row offset 1200
    push.1200 dup.1 add.24 mem_store
    dup.2 add.52 dup.1 add.25 mem_store
    drop

    # AIR 3: its pair slots start at table + 2 * pos_by_id[3]
    dup add.3 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 2 blocks at row offset 592
    push.592 dup.1 add.4 mem_store
    dup.2 add.44 dup.1 add.5 mem_store
    # aux: 2 blocks at row offset 1248
    push.1248 dup.1 add.24 mem_store
    dup.2 add.44 dup.1 add.25 mem_store
    drop

    # AIR 4: its pair slots start at table + 2 * pos_by_id[4]
    dup add.4 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 10 blocks at row offset 608
    push.608 dup.1 add.4 mem_store
    dup.2 add.60 dup.1 add.5 mem_store
    # aux: 6 blocks at row offset 1264
    push.1264 dup.1 add.24 mem_store
    dup.2 add.52 dup.1 add.25 mem_store
    drop

    # AIR 5: its pair slots start at table + 2 * pos_by_id[5]
    dup add.5 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 12 blocks at row offset 688
    push.688 dup.1 add.4 mem_store
    dup.2 add.64 dup.1 add.5 mem_store
    # aux: 14 blocks at row offset 1312
    push.1312 dup.1 add.24 mem_store
    dup.2 add.68 dup.1 add.25 mem_store
    drop

    # AIR 6: its pair slots start at table + 2 * pos_by_id[6]
    dup add.6 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 8 blocks at row offset 784
    push.784 dup.1 add.4 mem_store
    dup.2 add.56 dup.1 add.5 mem_store
    # aux: 2 blocks at row offset 1424
    push.1424 dup.1 add.24 mem_store
    dup.2 add.44 dup.1 add.25 mem_store
    drop

    # AIR 7: its pair slots start at table + 2 * pos_by_id[7]
    dup add.7 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 6 blocks at row offset 848
    push.848 dup.1 add.4 mem_store
    dup.2 add.52 dup.1 add.5 mem_store
    # aux: 4 blocks at row offset 1440
    push.1440 dup.1 add.24 mem_store
    dup.2 add.48 dup.1 add.25 mem_store
    drop

    # AIR 8: its pair slots start at table + 2 * pos_by_id[8]
    dup add.8 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 6 blocks at row offset 896
    push.896 dup.1 add.4 mem_store
    dup.2 add.52 dup.1 add.5 mem_store
    # aux: 6 blocks at row offset 1472
    push.1472 dup.1 add.24 mem_store
    dup.2 add.52 dup.1 add.25 mem_store
    drop

    # AIR 9: its pair slots start at table + 2 * pos_by_id[9]
    dup add.9 mem_load mul.2 dup.2 add
    # => [base, pos_ptr, table]
    # main: 12 blocks at row offset 944
    push.944 dup.1 add.4 mem_store
    dup.2 add.64 dup.1 add.5 mem_store
    # aux: 8 blocks at row offset 1520
    push.1520 dup.1 add.24 mem_store
    dup.2 add.56 dup.1 add.25 mem_store
    drop
    drop drop
end

#! Processes one PVM row of out-of-domain evaluations, scattering it to canonical addresses.
#!
#! The row is the LMCS-aligned wire sequence used by the lifted PCS, in commitment-group order:
#!
#! - 8 preprocessed extension-field slots;
#! - 512 main extension-field slots across 10 chiplets in proof order;
#! - 272 auxiliary-coordinate extension-field slots across 10 chiplets in proof order;
#! - 8 quotient extension-field slots.
#!
#! Each block is stored, folded into the DEEP fixed term with `horner_eval_ext`, and compressed
#! into the Eidos transcript. The absorb and the accumulation stay positional over the advice
#! stream; the only thing the proof order moves is where each segment is stored, which the table
#! staged by `stage_ood_scatter_table` supplies.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 1600, alpha_ptr, acc0', acc1']
pub proc process_row_ood_evaluations(
    state: types::EidosState,
    evaluation: types::HornerState,
) -> (types::EidosState, types::HornerState)
    dup.12 exec.layout::ood_scatter_table_ptr mem_store
    # preprocessed group, sole occupant: 2 blocks at row offset 0
    exec.layout::ood_scatter_table_ptr mem_load
    swap.13 drop
    exec.pipe_2
    # main group, proof position 0
    exec.layout::ood_scatter_table_ptr dup add.4 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.5 mem_load
    dynexec
    # main group, proof position 1
    exec.layout::ood_scatter_table_ptr dup add.6 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.7 mem_load
    dynexec
    # main group, proof position 2
    exec.layout::ood_scatter_table_ptr dup add.8 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.9 mem_load
    dynexec
    # main group, proof position 3
    exec.layout::ood_scatter_table_ptr dup add.10 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.11 mem_load
    dynexec
    # main group, proof position 4
    exec.layout::ood_scatter_table_ptr dup add.12 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.13 mem_load
    dynexec
    # main group, proof position 5
    exec.layout::ood_scatter_table_ptr dup add.14 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.15 mem_load
    dynexec
    # main group, proof position 6
    exec.layout::ood_scatter_table_ptr dup add.16 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.17 mem_load
    dynexec
    # main group, proof position 7
    exec.layout::ood_scatter_table_ptr dup add.18 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.19 mem_load
    dynexec
    # main group, proof position 8
    exec.layout::ood_scatter_table_ptr dup add.20 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.21 mem_load
    dynexec
    # main group, proof position 9
    exec.layout::ood_scatter_table_ptr dup add.22 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.23 mem_load
    dynexec
    # aux group, proof position 0
    exec.layout::ood_scatter_table_ptr dup add.24 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.25 mem_load
    dynexec
    # aux group, proof position 1
    exec.layout::ood_scatter_table_ptr dup add.26 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.27 mem_load
    dynexec
    # aux group, proof position 2
    exec.layout::ood_scatter_table_ptr dup add.28 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.29 mem_load
    dynexec
    # aux group, proof position 3
    exec.layout::ood_scatter_table_ptr dup add.30 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.31 mem_load
    dynexec
    # aux group, proof position 4
    exec.layout::ood_scatter_table_ptr dup add.32 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.33 mem_load
    dynexec
    # aux group, proof position 5
    exec.layout::ood_scatter_table_ptr dup add.34 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.35 mem_load
    dynexec
    # aux group, proof position 6
    exec.layout::ood_scatter_table_ptr dup add.36 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.37 mem_load
    dynexec
    # aux group, proof position 7
    exec.layout::ood_scatter_table_ptr dup add.38 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.39 mem_load
    dynexec
    # aux group, proof position 8
    exec.layout::ood_scatter_table_ptr dup add.40 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.41 mem_load
    dynexec
    # aux group, proof position 9
    exec.layout::ood_scatter_table_ptr dup add.42 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.43 mem_load
    dynexec
    # quotient group, sole occupant: 2 blocks at row offset 1584
    exec.layout::ood_scatter_table_ptr mem_load add.1584
    swap.13 drop
    exec.pipe_2
    exec.layout::ood_scatter_table_ptr mem_load add.1600
    swap.13 drop
end
