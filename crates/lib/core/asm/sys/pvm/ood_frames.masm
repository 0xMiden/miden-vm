# GENERATED by `cargo run -p miden-precompiles-verifier --release --features constants-tools --bin pvm-constants-regen -- --write` — do not edit by hand.
use miden::core::sys::pvm::layout

# Per-row OOD layout uses LMCS alignment 8:
#   preprocessed: 0 + 0 + 0 + 8 + 0 + 0 + 0 + 0 + 0 + 0 = 8 scalar evaluations
#   main:         104 + 128 + 72 + 8 + 40 + 48 + 32 + 24 + 24 + 48 = 528 scalar evaluations
#   aux:          48 + 40 + 24 + 8 + 24 + 56 + 8 + 16 + 24 + 32 = 280 scalar evaluations
#   quotient:     8 scalar evaluations
# The advice stream supplies 1648 base felts, read as 206 `adv_pipe` blocks,
# split into 22 segments (20 of them order-dependent).

#! Absorbs 2 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 16, alpha_ptr, acc0', acc1']
proc pipe_2
    repeat.2
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 4 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 32, alpha_ptr, acc0', acc1']
proc pipe_4
    repeat.4
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 6 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 48, alpha_ptr, acc0', acc1']
proc pipe_6
    repeat.6
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 8 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 64, alpha_ptr, acc0', acc1']
proc pipe_8
    repeat.8
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 10 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 80, alpha_ptr, acc0', acc1']
proc pipe_10
    repeat.10
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 12 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 96, alpha_ptr, acc0', acc1']
proc pipe_12
    repeat.12
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 14 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 112, alpha_ptr, acc0', acc1']
proc pipe_14
    repeat.14
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 18 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 144, alpha_ptr, acc0', acc1']
proc pipe_18
    repeat.18
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 26 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 208, alpha_ptr, acc0', acc1']
proc pipe_26
    repeat.26
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Absorbs 32 advice blocks into the row already targeted by the caller.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 256, alpha_ptr, acc0', acc1']
proc pipe_32
    repeat.32
        adv_pipe
        horner_eval_ext
        compress
    end
end

#! Stages the proof-order-dependent half of the out-of-domain scatter table.
#!
#! The proof commits each chiplet's trace at its position in the height-sorted proof order, while
#! the canonical constraint circuit reads it at the chiplet's instance offset. Both the canonical
#! destination and the segment length are compile-time per chiplet; this procedure only routes them
#! to the stream position the absorbed trace heights put that chiplet in.
#!
#! Must run after the AIR heights are stored and before the first out-of-domain row is ingested.
#!
#! Inputs:  []
#! Outputs: []
pub proc stage_ood_scatter_table
    procref.pipe_2 exec.layout::ood_scatter_table_ptr add.44 mem_storew_le dropw
    procref.pipe_4 exec.layout::ood_scatter_table_ptr add.48 mem_storew_le dropw
    procref.pipe_6 exec.layout::ood_scatter_table_ptr add.52 mem_storew_le dropw
    procref.pipe_8 exec.layout::ood_scatter_table_ptr add.56 mem_storew_le dropw
    procref.pipe_10 exec.layout::ood_scatter_table_ptr add.60 mem_storew_le dropw
    procref.pipe_12 exec.layout::ood_scatter_table_ptr add.64 mem_storew_le dropw
    procref.pipe_14 exec.layout::ood_scatter_table_ptr add.68 mem_storew_le dropw
    procref.pipe_18 exec.layout::ood_scatter_table_ptr add.72 mem_storew_le dropw
    procref.pipe_26 exec.layout::ood_scatter_table_ptr add.76 mem_storew_le dropw
    procref.pipe_32 exec.layout::ood_scatter_table_ptr add.80 mem_storew_le dropw

    exec.layout::proof_order_positions_ptr mem_load
    # => [pos]
    # main: 26 blocks at row offset 16
    push.16 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.76 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 12 blocks at row offset 1072
    push.1072 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.64 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.1 mem_load
    # => [pos]
    # main: 32 blocks at row offset 224
    push.224 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.80 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 10 blocks at row offset 1168
    push.1168 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.60 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.2 mem_load
    # => [pos]
    # main: 18 blocks at row offset 480
    push.480 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.72 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 6 blocks at row offset 1248
    push.1248 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.52 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.3 mem_load
    # => [pos]
    # main: 2 blocks at row offset 624
    push.624 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.44 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 2 blocks at row offset 1296
    push.1296 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.44 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.4 mem_load
    # => [pos]
    # main: 10 blocks at row offset 640
    push.640 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.60 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 6 blocks at row offset 1312
    push.1312 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.52 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.5 mem_load
    # => [pos]
    # main: 12 blocks at row offset 720
    push.720 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.64 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 14 blocks at row offset 1360
    push.1360 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.68 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.6 mem_load
    # => [pos]
    # main: 8 blocks at row offset 816
    push.816 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.56 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 2 blocks at row offset 1472
    push.1472 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.44 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.7 mem_load
    # => [pos]
    # main: 6 blocks at row offset 880
    push.880 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.52 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 4 blocks at row offset 1488
    push.1488 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.48 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.8 mem_load
    # => [pos]
    # main: 6 blocks at row offset 928
    push.928 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.52 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 6 blocks at row offset 1520
    push.1520 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.52 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop

    exec.layout::proof_order_positions_ptr add.9 mem_load
    # => [pos]
    # main: 12 blocks at row offset 976
    push.976 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.4 mem_store
    exec.layout::ood_scatter_table_ptr add.64 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.5 mem_store
    # aux: 8 blocks at row offset 1568
    push.1568 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.24 mem_store
    exec.layout::ood_scatter_table_ptr add.56 dup.1 mul.2 exec.layout::ood_scatter_table_ptr add add.25 mem_store
    drop
end

#! Processes one PVM row of out-of-domain evaluations, scattering it to canonical addresses.
#!
#! The row is the LMCS-aligned wire sequence used by the lifted PCS, in commitment-group order:
#!
#! - 8 preprocessed extension-field slots;
#! - 528 main extension-field slots across 10 chiplets in proof order;
#! - 280 auxiliary-coordinate extension-field slots across 10 chiplets in proof order;
#! - 8 quotient extension-field slots.
#!
#! Each block is stored, folded into the DEEP fixed term with `horner_eval_ext`, and compressed
#! into the Eidos transcript. The absorb and the accumulation stay positional over the advice
#! stream; the only thing the proof order moves is where each segment is stored, which the table
#! staged by `stage_ood_scatter_table` supplies.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr + 1648, alpha_ptr, acc0', acc1']
pub proc process_row_ood_evaluations
    dup.12 exec.layout::ood_scatter_table_ptr mem_store
    # preprocessed group, sole occupant: 2 blocks at row offset 0
    exec.layout::ood_scatter_table_ptr mem_load
    swap.13 drop
    exec.pipe_2
    # main group, proof position 0
    exec.layout::ood_scatter_table_ptr dup add.4 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.5 mem_load
    dynexec
    # main group, proof position 1
    exec.layout::ood_scatter_table_ptr dup add.6 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.7 mem_load
    dynexec
    # main group, proof position 2
    exec.layout::ood_scatter_table_ptr dup add.8 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.9 mem_load
    dynexec
    # main group, proof position 3
    exec.layout::ood_scatter_table_ptr dup add.10 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.11 mem_load
    dynexec
    # main group, proof position 4
    exec.layout::ood_scatter_table_ptr dup add.12 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.13 mem_load
    dynexec
    # main group, proof position 5
    exec.layout::ood_scatter_table_ptr dup add.14 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.15 mem_load
    dynexec
    # main group, proof position 6
    exec.layout::ood_scatter_table_ptr dup add.16 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.17 mem_load
    dynexec
    # main group, proof position 7
    exec.layout::ood_scatter_table_ptr dup add.18 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.19 mem_load
    dynexec
    # main group, proof position 8
    exec.layout::ood_scatter_table_ptr dup add.20 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.21 mem_load
    dynexec
    # main group, proof position 9
    exec.layout::ood_scatter_table_ptr dup add.22 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.23 mem_load
    dynexec
    # aux group, proof position 0
    exec.layout::ood_scatter_table_ptr dup add.24 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.25 mem_load
    dynexec
    # aux group, proof position 1
    exec.layout::ood_scatter_table_ptr dup add.26 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.27 mem_load
    dynexec
    # aux group, proof position 2
    exec.layout::ood_scatter_table_ptr dup add.28 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.29 mem_load
    dynexec
    # aux group, proof position 3
    exec.layout::ood_scatter_table_ptr dup add.30 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.31 mem_load
    dynexec
    # aux group, proof position 4
    exec.layout::ood_scatter_table_ptr dup add.32 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.33 mem_load
    dynexec
    # aux group, proof position 5
    exec.layout::ood_scatter_table_ptr dup add.34 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.35 mem_load
    dynexec
    # aux group, proof position 6
    exec.layout::ood_scatter_table_ptr dup add.36 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.37 mem_load
    dynexec
    # aux group, proof position 7
    exec.layout::ood_scatter_table_ptr dup add.38 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.39 mem_load
    dynexec
    # aux group, proof position 8
    exec.layout::ood_scatter_table_ptr dup add.40 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.41 mem_load
    dynexec
    # aux group, proof position 9
    exec.layout::ood_scatter_table_ptr dup add.42 mem_load swap mem_load add
    swap.13 drop
    exec.layout::ood_scatter_table_ptr add.43 mem_load
    dynexec
    # quotient group, sole occupant: 2 blocks at row offset 1632
    exec.layout::ood_scatter_table_ptr mem_load add.1632
    swap.13 drop
    exec.pipe_2
    exec.layout::ood_scatter_table_ptr mem_load add.1648
    swap.13 drop
end
